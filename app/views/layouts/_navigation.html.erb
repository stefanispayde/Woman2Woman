

<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Woman2Woman</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if admin_signed_in? %>
        <li class="dropdown pc-menu">
          <a id="admin-settings" class="dropdown-toggle" data toggle="dropdown" href="#">
            <span id="admin-name"><% current_admin.name %></span>
            <span class="caret"></span>
          </a>

          <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Edit Profile', edit_admin_registration_path %></li>
              <li><%= link_to 'Log out', destroy_admin_session_path, method: :delete %></li>
            </ul>
          </li>

          <li class="mobile-menu">
            <%= link_to 'Edit Profile', edit_admin_registration_path %>
          </li>
          <li class="mobile-menu">
            <%= link_to 'Log out', destroy_admin_session_path, method: :delete %>
          </li>


          <% else # admin not signed it %>
         <li class="nav-links"><%= link_to 'Signin', new_admin_session_path %></li>
         <li class="nav-links"><%= link_to 'Signup', new_admin_registration_path %></li>
       <% end # if admin is signed in %>



      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
